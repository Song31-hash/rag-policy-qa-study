embedding_model: "text-embedding-3-small"
embedding_dimension: null

llm_model: "gpt-4o-mini"
temperature: 0

paths:
  raw: "data/raw"
  processed: "data/processed_overlap_64"
  dataset: "data/dataset"
  indexes: "data/indexes_overlap_64"
  experiments: "experiments"
  results: "results"

  output_dir: "experiments/overlap_64_topk_3"
  chunks_path: "data/processed_overlap_64/chunks.json"
  index_dir: "data/indexes_overlap_64"
  questions_path: "data/dataset/questions.json"
  gold_path: "data/dataset/gold_answers.json"

chunk_size: 512
chunk_overlap: 64
top_k: 3
use_retrieval: true